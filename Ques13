#include <stdio.h>

int main()
{

	char s[20];
	printf("Enter a string:");
	fgets(s, sizeof(s), stdin);

	int count=0;

	for(int i=0; s[i]!='\0'; i++)
	{
	    if(s[i]=='\n')
	    break;
	    
		count++;
	}

	printf("The size of string is :%d",count);

	return 0;
}
